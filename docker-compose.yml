version: '3.8'

services:
  home-app:
    container_name: home_app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8282:8282"
    volumes:
      - "${HOST_DATA_DIR:-./data}:/data"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
